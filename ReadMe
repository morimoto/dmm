============== -*- outline -*- ==============

                   ReadMe
2008/09  morimoto
=============================================

====================
* dmm とは
====================

	スペル自体にはあまり意味はないけど，Memory Management for Development みたいな感じ
	mmap を使用してメモリを覗いたり変更するので root 権限で使用しないといけない
	でも、メモリダンプとメモリエディットができる程度

====================
* コンパイル方法
====================

	クロスコンパイルをする場合、sample.config を .config にリネームし
	クロスコンパイラを指定してください。

	> cp sample.config  .config
	> vi .config

	その後 make し、dmm をコピー

	> make
	> cp dmm ${PATH}

-----------------------------
** スタティックリンクしたい場合
-----------------------------

	EXTR=-static make
